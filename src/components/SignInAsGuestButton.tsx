"use client";

import { Button } from "./ui/Button";

export function SignInAsGuestButton() {
  async function handleSignInAsGuest() {
    try {
    } catch (error) {}
  }

  return <Button variant="ghost">Explore as a guest</Button>;
}
